services:
  redis-stack:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"
    # volumes:
    #   - /data/docker/redis-stack-lixi-current:/data
  postgres:
    image: timescale/timescaledb:latest-pg17
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=atishadbpass
    # volumes:
    #   - /data/docker/timescaledb-lixi-current:/var/lib/postgresql/data

